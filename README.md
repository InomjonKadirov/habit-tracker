# habit-tracker

habit-tracker is a small REST API for tracking daily habits. Built with FastAPI and SQLAlchemy, it lets you create and archive habits, record daily check-ins, and view streak and completion statistics for each habit over time.

## Installation

```bash
uv sync
```

## Running

```bash
uv run uvicorn app.main:app
```